{
  "name": "soufit-tools",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "migrate:storage": "ts-node -r tsconfig-paths/register --project tsconfig.json ./migrate-storage.ts",
    "validate:import": "ts-node -r tsconfig-paths/register --project tsconfig.json ./validate-import.ts",
    "simulate:days": "ts-node -r tsconfig-paths/register --project tsconfig.json ./simulate-days.ts"
  },
  "devDependencies": {
    "@types/node": "^20.7.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "~5.9.2"
  }
}
